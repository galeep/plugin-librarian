---
description: List your currently installed Claude Code plugins
allowed-tools: ["Bash"]
---

# Installed Plugins

Show all currently installed plugins, grouped by marketplace.

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/run-librarian" installed -v
```

Present the list in a clean format for the user.
