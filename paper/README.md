# Artifact: data, scripts and results

This directory holds the released material for "Librarian Catches Thief:
Surfacing Supply Chain Attack Campaigns via Document Similarity in an AI
Agent Skill Registry". The tool itself is the repository this directory
sits in; everything here is the data, the analysis scripts and their
recorded outputs.

This directory sits at the repository root, beside `librarian/`. Run the
scripts from inside a full clone: each locates `paper/iocs.json` and the
`librarian` package by walking up from its own directory, so a script
copied elsewhere will not run. Paths in this file are relative to the
repository root.

`paper/MANIFEST.txt` lists every file with its size and SHA-256 digest,
one file per line as path, bytes, digest, so `shasum -a 256` on any
listed file can be compared directly.

```
paper/iocs.json                    ground truth: flagged publisher accounts, skill slugs (the
                                   directory name a skill is published under), payload indicators
paper/supplementary/               commit table, similarity matrix, scaffold list, reproduction commands
paper/sources/scans/               archived scans, analysis scripts, dated results files
paper/sources/baseline-comparisons.md   Section 5.2 baseline summary
paper/figures/                     figure generators
```

## Start here

1. From the repository root, in a virtual environment, run `pip install -e .`.
   That installs the `librarian` package, its `librarian` command and
   `datasketch`.
2. Export `LIBRARIAN_CORPUS` pointing at a corpus checkout laid out as "Corpus
   checkout" below describes; the commands in this artifact write it as
   `$LIBRARIAN_CORPUS`. Several scripts need no corpus at all; "Pinning the
   corpus" names them.
3. Run one of those, `python paper/sources/scans/table1_marketplaces.py`, and
   compare its output with the shipped
   `paper/sources/scans/table1-marketplaces-20260907.md`. That script also
   takes `--check`, which verifies the shipped file instead of rewriting it.

## What the paper promises and what satisfies it

| Sentence in the paper | What it promises | Files here |
|---|---|---|
| "The scanning tool and dataset are released as open source for reproducibility." (Section 1) | tool and dataset | the `librarian/` package in this repository; `paper/sources/scans/scan_20260314_threshold*_skillonly.json` |
| "That repository is the artifact: it holds the tool, the commit table, the similarity matrix, the scaffold-cluster list, the results files behind the figures reported here, and the reproduction commands with the tool commit the published scan ran at." (Appendix C) | six named items | `librarian/`; `paper/supplementary/repository-commits.md`; `paper/supplementary/marketplace-similarity-matrix.md`; `paper/supplementary/suspicious-scaffold-clusters.md`; the dated results files in `paper/sources/scans/`; `paper/supplementary/reproduction-steps.md` |
| "grep-based behavioral scanning ... with the patterns in the released script" (Section 3.5) | the six Table 7 patterns, in runnable form | `paper/sources/scans/behavioral_scan.py`, `paper/sources/scans/behavioral-scan-20260907.md` |
| "the six patterns of Table 7, recorded in the released script" (Section 4.5) | same six patterns behind the overlap figures | as above, plus `paper/sources/scans/rq3_hit_value.py`, `paper/sources/scans/rq3-hit-value-20260907.md` |
| "the artifact and its reproduction commands are described in Appendix C" (Section 3.6) | commands that rerun the scan | `paper/supplementary/reproduction-steps.md` |
| "as the artifact's commit table records" (Section 4.2) | per-repository snapshot SHAs | `paper/supplementary/repository-commits.md` |
| "The full matrix is given with the artifact." (Section 4.2) | the marketplace similarity matrix | `paper/supplementary/marketplace-similarity-matrix.md`, generated by `paper/sources/scans/marketplace_matrix.py` |
| "per-cluster results are among the files released with the code" (Section 4.4) | per-cluster precision detail | `paper/sources/scans/file_level_precision.py`, `paper/sources/scans/file-level-precision-20260907.md`, `paper/sources/scans/table4_triage_record.py`, `paper/sources/scans/table4-triage-record-20260907.md` |
| "Only the ≥ 20 and ≥ 10 rows reproduce from the released ground truth." (Table 4 caption) | the ground truth those rows use | `paper/iocs.json`; the rows are Table 4's cluster-size bands, clusters of at least 20 and at least 10 files |
| "the default seed reproduces the archived scan exactly" (Section 3.3) | the archived scan, and the seed sweep | `paper/sources/scans/scan_20260314_threshold90_skillonly.json`; `paper/sources/scans/seed_robustness.py` and `seed-robustness-20260907.md`; `paper/sources/scans/recompute_ari.py`, which recomputes the Adjusted Rand Index between two clusterings, and the `recompute-ari-results-20260907.json` file it writes |
| "Further campaign-consistent but unconfirmed scaffold clusters accompany the artifact." (Appendix B) | the scaffold-cluster list | `paper/supplementary/suspicious-scaffold-clusters.md` |
| "all quantitative results come from deterministic runs of the released tool and scripts" (Section 3.6) | every script behind a published number | all scripts in `paper/sources/scans/` and `paper/figures/` |

## Which script stands behind which table or figure

| Paper object | Script | Recorded output |
|---|---|---|
| Table 1, corpus repositories | `paper/sources/scans/table1_marketplaces.py` | `table1-marketplaces-20260907.md` |
| Table 2, threshold sensitivity | `paper/sources/scans/table2_distinct_files.py`, `paper/sources/scans/analyze_sweep.py` | `table2-distinct-files-20260907.md`, `threshold-sweep-20260314.md` |
| Table 4, precision and recall | `paper/sources/scans/file_level_precision.py`, `paper/sources/scans/table4_triage_record.py` | `file-level-precision-20260907.md`, `table4-triage-record-20260907.md` |
| Table 5, shingle ablation | `paper/sources/scans/shingle_ablation.py` | `shingle-ablation-20260907.md` |
| Table 7, behavioral patterns | `paper/sources/scans/behavioral_scan.py` | `behavioral-scan-20260907.md` |
| Figure 1, threshold sensitivity | `paper/figures/generate_figures.py` | `threshold-sweep-20260314.md` supplies the plotted values |
| Figure 2, cluster size distribution | `paper/figures/generate_cluster_distribution.py` | reads `paper/sources/scans/scan_20260314_threshold90_skillonly.json` directly and writes the figure PDF; no PDF ships here, and two runs of the generator produce identical bytes |
| Figure 3, method comparison | `paper/figures/generate_figures.py` | `paper/sources/baseline-comparisons.md` supplies the plotted values |
| Section 3.3, seed and order robustness | `paper/sources/scans/seed_robustness.py`, `paper/sources/scans/order_permutation.py`, `paper/sources/scans/recompute_ari.py` | `seed-robustness-20260907.md`, `order-permutation-20260907.md`, `reproduction-20260907.md`, `recompute-ari-results-20260907.json` |
| Section 3.2 and Appendix C, backup-path filter | `paper/sources/scans/backup_slug_check.py` | `backup-slug-check-20260907.md` |
| Section 4.1, clustering reproduced from the pinned corpus | `paper/sources/scans/cluster_gap_recompute.py` | `reproduction-20260907.md`, section a |
| Section 4.5, pattern and cluster overlap | `paper/sources/scans/rq3_hit_value.py` | `rq3-hit-value-20260907.md` |
| Section 5.2, exact-hash baseline | `paper/sources/scans/exact_hash_baseline.py` | `exact-hash-baseline-20260907.md` |
| Section 5.2, TF-IDF baseline | `paper/sources/scans/tfidf_comparison.py` | `tfidf-comparison-20260907.md` |
| Section 5.2, sentence-embedding baseline | `paper/sources/scans/embedding_comparison.py` with `embedding_comparison_report.py` | `embedding-comparison-20260907.md` |
| Precision by size band, benign content in attributed clusters, payload-indicator recall, filter effects, and the denominator reconciliation | `paper/sources/scans/supplementary_data_tables.py` | `paper/supplementary/supplementary-data-tables.md` |

## The tool commit the published scan ran at

The scan reported in the paper ran with the backup filter as this
repository has it at commit `b55ceff1fde1ef0678a62067d7e0a6cdac852b3e`,
the state of the tool when the published scan ran. That tree is what the
published scan used, so no checkout is needed to reproduce it: the rule
skips any path whose string contains `backup`.

The released code keeps that substring rule. A revised rule restricted to
a top-level `backup` or `backups` directory of the scan root would index
84 more files and move no published count; that measurement, not the
revised rule, is what ships, in
`paper/sources/scans/backup-slug-check-20260907.md`.
`paper/supplementary/reproduction-steps.md` states the same thing.

## Pinning the corpus

Every script that reads file contents takes the corpus root from the
`LIBRARIAN_CORPUS` environment variable, which has no default. Set it to
a checkout pinned to the snapshot the paper used, never to a live clone:
the upstream repositories have drifted. A script whose work needs the
corpus exits non-zero with a one-line message when the variable is unset
or names no directory, rather than reporting numbers computed against the
wrong tree.

### Corpus checkout

`LIBRARIAN_CORPUS` must name a directory holding one sub-directory per
corpus repository, named exactly as `paper/supplementary/repository-commits.md`
names it, each checked out at the commit that table records. The ClawHub
archive is `clawhub-archive/`, at commit `16c991de`.

Scripts address a file as `$LIBRARIAN_CORPUS/<marketplace>/<path>`, where `<path>`
is the file's path inside that repository as the archived scan's
`file_index` records it. In the ClawHub archive that shape is
`clawhub-archive/skills/<account>/<slug>/SKILL.md`.

Provenance rows show commands as you would type them: `$LIBRARIAN_CORPUS` for
the corpus directory and `python` for whichever interpreter you are using.
One redaction remains. `<private-history>` stands for a commit that exists
only in the authors' private drafting repository and cannot be resolved from
this one. The corpus is pinned by
`paper/supplementary/repository-commits.md`, not by where it sat on anyone's
disk.

The ClawHub archive was cloned on 2026-03-13 at commit `16c991de`. That
upstream repository has since been removed from GitHub, but Software
Heritage preserves the snapshot: revision
`swh:1:rev:16c991dea171b9f4e785cb2a70dc1ae2ade83396`, corpus tree
`swh:1:dir:0062bcb4b60e717abcccc9ee548f78957bbec93b`, retrievable
through the vault API. `paper/supplementary/reproduction-steps.md` gives the
vault commands. The per-repository SHAs for the community and curated
marketplaces are in `paper/supplementary/repository-commits.md`; check each
sub-clone out at its recorded SHA rather than at whatever its default
branch now points to.

Four scripts read only the archived scan JSON and `iocs.json` and need
no corpus at all: `analyze_sweep.py`, `marketplace_matrix.py`,
`table1_marketplaces.py` and `table2_distinct_files.py`. `table4_triage_record.py` computes every
figure it reports from the scan and `iocs.json` as well; it imports
`file_level_precision.py` for the attribution rule.
`file_level_precision.py` needs a corpus only for its exploratory
content-marker column; its two account rules read the scan alone. Every
other script needs the pinned corpus, and `behavioral_scan.py` exits
non-zero before writing anything if `LIBRARIAN_CORPUS` is unset or if
the on-disk `SKILL.md` count under the ClawHub archive is not 23,806.

## Populations and denominators

Five figures recur in the recall sentences, on four different denominators.
The paper's own wording defines each; this table says which one and names the
file here that carries it.

| Figure | What it counts | Where it comes from |
|---|---|---|
| 341 | Skill slugs in the indicator-of-compromise (IOC) list published by Koi Security with its ClawHavoc report, cited in the paper's references. The paper: "Of the 341 skill slugs in Koi Security's original ClawHavoc IOC list". | That list is not redistributed here. `iocs.json` records the payload infrastructure and the accounts, not that list. |
| 337 | Of those 341, the ones present in the archive. The paper: "337 resided in the ClawHub archive. All 337 appeared in similarity clusters (100% within-archive recall)", giving full-list recall of 337/341 = 98.8%. The other 4 were absent, 3 likely removed before the snapshot and 1 a probable naming discrepancy. | The same external list, not in this artifact. |
| 354 | This study's own enumeration of one account's skills, used where an experiment is scoped to that account. The paper: "the narrower hightower6eu subset (354 IOC slugs) because those experiments were scoped to a single author". | `paper/iocs.json`, `clawhub_authors.hightower6eu`: 353 slugs with a payload and 1 without, and a `total_skills` of 354. |
| 352 | The 354 minus the 2 the scanner's backup-path filter skipped. The paper: "Of the 354 slugs, 2 were skipped by the backup-path filter". That filter is the one described in Section 3.2, which "skipped any path containing backup (864 files, 780 of them one repository's backup directory)". | `paper/sources/scans/backup-slug-check-20260907.md`, which locates both skipped files on disk and measures what the filter cost. |
| 351 | Of the 352 scanned, the ones that appear in a cluster. The paper: "of the 352 scanned, 351 appeared in clusters (99.7% of those scanned; 99.2% against the full 354)". | `paper/sources/scans/shingle-ablation-20260907.md`, where 351/352 = 99.7% is the recall column at every shingle size, cross-checked against the printed table. |

So 99.7% and 99.2% are the same numerator over different denominators, and
98.8% is a different numerator over a third. The two smaller denominators, 341
and 337, rest on an external list this artifact does not carry, so nothing
here recomputes them; the three that this artifact does carry are 354, 352 and
351. All five are reconciled in one place in
`paper/supplementary/supplementary-data-tables.md`, section "Populations and
denominators".

Whether the tokenizer reads the YAML frontmatter, and whether the
100-character minimum applies before or after tokenization, are answered in
`paper/supplementary/supplementary-data-tables.md`, section "Filter effects:
tokenizer, frontmatter, and the 100-character threshold".

## Interpreter and packages

Python 3.10 or later; the type annotations use `tuple[str, ...]` and
`list[Capability]` syntax. From the repository root, in a virtual
environment, `pip install -e .` installs the `librarian` package, its
`librarian` command and `datasketch`. That command is the one
`paper/supplementary/reproduction-steps.md` invokes. Add `scikit-learn`,
`matplotlib`, `numpy` and `sentence-transformers` as the table below
requires; none of the four is a dependency of the package.

| Script | Needs beyond the standard library |
|---|---|
| `order_permutation.py`, `seed_robustness.py`, `recompute_ari.py`, `shingle_ablation.py`, `backup_slug_check.py`, `cluster_gap_recompute.py` | `datasketch`, plus the `librarian` package, which `pip install -e .` at the repository root provides |
| `tfidf_comparison.py` | `scikit-learn` |
| `embedding_comparison.py`, `embedding_comparison_report.py` | `sentence-transformers` and `torch`, imported lazily inside the run |
| `paper/figures/generate_figures.py`, `paper/figures/generate_cluster_distribution.py` | `matplotlib`, `numpy` |
| `analyze_sweep.py`, `behavioral_scan.py`, `exact_hash_baseline.py`, `file_level_precision.py`, `marketplace_matrix.py`, `rq3_hit_value.py`, `table1_marketplaces.py`, `table2_distinct_files.py`, `table4_triage_record.py` | standard library only, though several import a sibling script in this directory |

`embedding_comparison.py` needs `sentence-transformers`, which brings torch.
The model is `all-MiniLM-L6-v2` at the snapshot recorded in
`paper/sources/scans/embedding-comparison-20260907.md`, whose provenance table also
lists the package versions the published run used. Encoding, normalisation and
the cosine similarity are computed in torch throughout, so no NumPy
interoperability is required; any environment with a working
`sentence-transformers` will do.

A script that writes a results file names it for the current UTC date, so
a rerun never overwrites the dated file shipped here. The file each
script writes is the recorded output named in the table above. Set
`SCAN_RESULTS_OUT` to a full path to direct a rerun somewhere specific.

## Reproducing on Linux

The corpus was checked out on macOS, whose default filesystem is
case-insensitive. Thirty-five case-insensitive path collisions occurred
in that checkout, affecting 28 files: two paths differing only in case
collapsed onto one file, so one of each pair was never scanned. Those two
counts are recorded in the paper's Limitations section; no script here
measures them, and no file here lists the 28.

A Linux checkout is case-sensitive and will therefore contain those 28
files where the macOS checkout did not. A rerun on Linux will index
slightly more files than the archived scan and will not reproduce it
byte for byte. The archived scan JSONs shipped here are the macOS run
and are the reference for every published number. Compare against them
rather than against a fresh Linux clone.

## The archived scans

Six scans at thresholds 70, 75, 80, 85, 90 and 95 percent back Table 2
and Figure 1. The 90 percent scan is the primary analysis and the input
to most scripts here. Each carries a `file_index` of 31,634 entries with
relative marketplace paths, a `cluster` list, and a `marketplace_index`
covering only marketplaces with at least one clustered file. The six
carry no local paths and no usernames, so a rerun can be diffed against
them directly.
